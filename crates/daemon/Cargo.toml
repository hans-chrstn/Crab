[package]
name = "daemon"
version = "0.1.0"
edition = "2024"

[dependencies]
hidapi = "2.6.6"
hid-protocol = { path = "../hid-protocol" }
serde = { version = "1.0.228", features = ["derive"]}
serde_json = { version = "1.0.150", default-features = false, features = ["alloc"] }
